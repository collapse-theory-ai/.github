name: Rust CI

on:
  pull_request:
  push:
    branches:
      - $default-branch

permissions:
  contents: read

jobs:
  verify:
    uses: collapse-theory-ai/.github/.github/workflows/reusable-rust-ci.yml@main
